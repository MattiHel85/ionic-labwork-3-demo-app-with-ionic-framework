import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demo-app-with-ionic-framework',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
